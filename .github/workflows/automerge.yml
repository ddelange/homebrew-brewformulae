name: automerge
on:
  # trigger when checks complete
  check_run:
    types: [completed]
  # trigger when checks already passed and a required review is submitted
  pull_request_review:
    types: [submitted]
  # trigger when draft PR is converted to PR, or labels change
  pull_request:
    types: [ready_for_review, labeled, unlabeled]

jobs:
  automerge:
    runs-on: ubuntu-latest
    steps:
      - name: automerge
        uses: pascalgn/automerge-action@master
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}  # provided by Workflow runner
          MERGE_FILTER_AUTHOR: ddelange
          MERGE_METHOD: squash
          MERGE_RETRIES: 0
          MERGE_LABELS: '!WIP'  # merge the PR when branch protection allows it (checks, reviews, draft, ...)
          UPDATE_LABELS: ''  # merge in upstream when upstream has "Require branches to be up to date before merging"
          MERGE_DELETE_BRANCH: true
          UPDATE_RETRIES: 0
